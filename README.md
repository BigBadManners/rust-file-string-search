# rust-file-string-search
String search CLI written in Rust that searches for a regex string in a given directory's files (and sub-directories... and their sub-directories... and so on and so forth).

## External Crates
- walkdir
- regex
- structopt
- failure
- exitfailure
